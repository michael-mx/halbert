server:
  port: 8200
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clinic?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&serverTimezone=UTC
    username: root
    password: abc123
mybatis:
  type-aliases-package: com.halbert.model

#showSql
logging:
  level:
    com:
      example:
        mapper : debug